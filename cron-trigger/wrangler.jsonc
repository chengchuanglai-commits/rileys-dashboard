{
  // Cloudflare Worker —— 准点触发 GitHub trading-signals 工作流
  // 解决 GitHub schedule 延迟数小时/丢运行的问题（Cloudflare cron 秒级准点）
  "name": "rileys-signal-cron",
  "main": "src/index.js",
  "compatibility_date": "2026-01-01",
  "triggers": {
    // 11:30 UTC → trading-signals.yml(主信号,盘前) ; 12:00 UTC → deepseek-broad.yml(DeepSeek广撒)
    "crons": ["30 11 * * 1-5", "0 12 * * 1-5"]
  }
}
